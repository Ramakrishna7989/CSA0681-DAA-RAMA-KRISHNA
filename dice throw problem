#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {
    int dice, i;
    srand(time(0));

    for (i = 0; i < 10; i++) {
        dice = (rand() % 6) + 1;
        printf("Dice throw %d: %d\n", i+1, dice);
    }

    return 0;
}
